[railway]
volume = "/data"

[build]
builder = "NIXPACKS"

[[services]]
name = "backend"
dockerfile = "Dockerfile"
port = 5000
healthcheck = "/"
envGenerationMode = "lookup"

[[services]]
name = "frontend"
dockerfile = "Dockerfile"
port = 3000
healthcheck = "/"
